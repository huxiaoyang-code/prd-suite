# PRD Suite — 共享契约

| skill | 干什么 | 写 |
|---|---|---|
| requirement-alignment | 逐条对齐需求 | C / O / D 区、`explore/` |
| requirement-research | 查同类产品做法 | R 区 |
| prd-writing | 出 PRD 与 mockup | PRD.md、mockup.html、shots/ |
| prd-review | 校验 | 不写，只输出缺失清单 |

四个 skill 之间不互相调用。调研只产出对比与分析，**不自行写进 C 区**，方案由人在 alignment 环节拍板。

---

## 工作区

```
prd-workspace/
├── index.md                  进行中的需求
├── example-feature/
│   ├── alignment.md
│   ├── explore/              讨论期原型，不截图，归档时删
│   ├── PRD.md
│   ├── mockup.html
│   └── shots/                定稿快照，可全删重跑
└── archive/
```

目录名用英文短横线，一个需求只有一个，定了不改。

不存在就自建（建目录、写只有表头的 `index.md`）。**建之前告知将在哪个路径建什么，等确认。** 建完接着回答原本的请求，不停下来等用户重新提问。

**文件系统不可写时**：内容完整输出到对话并告知应存路径，不假装已保存；开场直接问「新需求还是接之前的」，接之前的请用户贴入 alignment.md。

---

## 一个 session 一个需求

用户每个需求开新 session。skill 要保证的只有一件事：**关掉再进来，读文件就能接上。**

### index.md

```markdown
# 进行中

| 目录 | 中文名 | 别称 | 状态 |
|---|---|---|---|
| example-feature | 示例功能 | 示例、demo | 讨论中，卡 O3 |
```

「别称」列是路由的匹配源：用户说的口语叫法，靠这一列找到对应目录（如 `example-feature/`）。

### 开场

**没确定目录之前不记录任何内容。**

| 情况 | 动作 |
|---|---|
| 命中中文名或别称 | 读该 alignment.md，先复述头部的三行摘要，确认后继续 |
| 没命中 | 说明将新建 `xxx/`，等确认，同时把用户刚才的叫法写进别称列 |
| 命中两条以上 | 列出候选让用户选 |
| 都没命中但用户坚称讨论过 | 查 `archive/` |

### 头部三行

每轮讨论结束时更新，这是下次进来唯一的接续依据。

```markdown
# 示例功能
别称：示例、demo

- 停在哪：③ 某个环节的规则已定完，④ 还没谈
- 卡在什么上：O3 某个问题没定，等相关方回复
- 回来第一件事：把 O3 的答案落进 C 区，然后接着谈 ④
```

---

## alignment.md

```markdown
## 一、已确认
C1  某条已确认的结论，具体到可被验证

## 二、未决
O1  某个待决问题
    选项：方案 A / 方案 B / 方案 C
    影响：决定某个页面的结构或某个计算口径
    谁定：业务

## 三、调研
R1  **某厂做法** —— 链接
    做法：一两句说清机制。
    踩的坑：对方遇到的具体问题。
    解法：对方后续的修补方式。
    → 关联 O1

## 四、已否决
D1  某个被否决的方案
    否决理由：具体说明为什么不行。｜2026-08-20
```

**编号永不复用。** C2 被推翻就移进 D 区，新结论取最大号 +1。编号不连续是允许的。

**推翻必须写理由和日期**，写清为什么不行，不写「已放弃」。用户没给理由就追问一句再落盘。

**未决点必须写「影响什么」和「谁定」。** 影响页面结构或计算口径的属于卡口径，其余可带进 PRD 的「待确认」章节。

**调研条目必须带链接和对方踩的坑。**

**跨需求的口径写引用不复制**：`C4 某口径的定义见 [example-feature C7]，若该口径变更需回查本需求相关页面`。

自己补的默认值可以写，末尾标注「(默认值，未经确认)」。

---

## 归档

PRD 交付且验收通过后，目录移进 `archive/` 加年月前缀，删掉 `explore/`，从 index.md 移除。

归档后不做二次沉淀，将来要引用其中的口径或否决理由，由用户自己进去找并引用编号。

---

## 跨 agent

frontmatter 只保留 `name` 和 `description`；不假设任何具体工具存在；不假设文件系统可写；skill 之间不互相调用。

**若 agent 不自动读取本文件**，需手工粘进各 skill，否则路由、编号规则、alignment.md 格式全部失效。

---

## 判据与补丁

判据（什么算写清楚了、什么必须问人、什么不进文档）不会过期，写进正文。

补丁（为弥补当前模型能力不足而写的流程约束）写在各 skill 末尾的补丁区，注明原因，模型变强后整段删除。
